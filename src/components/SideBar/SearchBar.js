import React from "react";

const SearchBar = () => {
  return (
    <div>
      <input style={{ padding: "5px" }} type="text" placeholder="search..." />
    </div>
  );
};

export default SearchBar;
